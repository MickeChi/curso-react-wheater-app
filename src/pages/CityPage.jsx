import React from 'react';
import PropTypes from 'prop-types';

const CityPage = props => {
    return (
        <div>
            City page
        </div>
    );
};

CityPage.propTypes = {
    
};

export default CityPage;