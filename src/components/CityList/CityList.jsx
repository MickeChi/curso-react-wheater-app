import React from 'react';
import PropTypes from 'prop-types';


 const CityList = (props) => {
    return (
        <div>
            City List
        </div>
    );
}

CityList.propTypes = {

};
export default CityList;
